let text_one = "Delion"
let text_two = " And Naveria"

// Concat or join the string
const concat_text = text_one + text_two

console.log(concat_text)

// To check data type, use typeof
console.log(typeof(concat_text))

// I can use backtick for more clean to insert a value in a string
console.log(`I use backtick in here: ${text_one + text_two}`)